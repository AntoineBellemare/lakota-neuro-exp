# --- Large local bundles (download separately, not versioned) ---
DSI-streamer-and-drivers/
DSI-streamer-and-drivers.zip

# --- LSL streamer binaries (see the `lsl-setup` branch) ---
dsi2lsl/

# --- Recorded data / pilots (kept local) ---
data/
test-pilot/

# --- Unrelated experiment bundle ---
All_together/

# --- MMBT extras not needed in-repo (drivers + quickguide ARE tracked) ---
# mmbt/TachyonSetup.exe
mmbt/MMBT-S_Flyer.pdf

# --- Editor / temp ---
.vscode/
scripts/temp-*.csv

# --- Python ---
__pycache__/
*.pyc

.venv/
